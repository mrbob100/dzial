{% extends '::base.html.twig' %}

{% block content -%}
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-9 slon container-fluid">
    <h1>Product creation</h1>

    {{ form(form) }}
            {#  form_widget(form, { 'attr': {'id': 'title-field'} })  #}
        <ul class="record_actions">
    <li>
        <a href="{{ path('vl_product') }}">
            Back to the list
        </a>
    </li>
</ul>
        </div>
    </div>
</div>
{% endblock %}
